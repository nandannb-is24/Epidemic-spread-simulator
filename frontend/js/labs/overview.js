/**
 * overview.js — Overview section (no logic needed; purely HTML-driven).
 * Placeholder to satisfy ES module import structure.
 */

export function initOverview() {
  // Overview is rendered statically in index.html.
  // Nothing to initialise here.
}
